#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<int>position(n),healths(n);
    for(int i=0;i<n;++i)
    cin>>position[i];
    for(int i=0;i<n;++i)
    cin>>healths[i];
    string direction;
    cin>>direction;
    cout<<direction;
    stack<int>st;
    vector<int>temp=position;

}